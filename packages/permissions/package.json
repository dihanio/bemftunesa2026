{
  "name": "@bemft/permissions",
  "version": "0.1.0",
  "private": true,
  "description": "Shared RBAC and permission policy engine",
  "main": "src/index.ts",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@bemft/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
